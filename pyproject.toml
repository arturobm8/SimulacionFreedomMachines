[project]
name = "freedom-machines"
version = "0.1.0"
description = "Multi-robot warehouse simulation framework"
requires-python = ">=3.11"

[tool.uv.workspace]
members = ["config", "core", "render", "base", "moop", "notebooks"]
